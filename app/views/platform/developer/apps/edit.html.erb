<%= render :partial => "/platform/developer/common/header" %>

<div class="section_title">
  <%= tr('Editing "{app_name}"', 'API', :app_name => @app.name) %> 
</div>

<div class="app_fields">
	<%= form_for(@app, :as => :application, :url => {:action => :update, :id => @app.id}, :html => {:method => :post, :multipart=>true}) do |form| %>
	  <%= render :partial=>'form', :locals=>{:form=>form} %>
	  
	  <div style="text-align:center; padding:15px;">
      <button type="submit" class="platform_btn platform_submit_btn">
          <span><%=tr('Save')%></span>
      </button>
	    <%=tr("or")%> <%=link_to(tr("Cancel"), :action => :index, :id => @app.id)%>
	  </div>
	<% end %>
</div>


<%= render :partial => "/platform/developer/common/footer" %>
